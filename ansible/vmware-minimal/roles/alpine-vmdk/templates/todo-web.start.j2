#!/bin/sh
# Defer app pull until network and VMware Tools are up (local.d runs 99-* last).
sleep 30
podman pull {{ todo_web_image }} || true
podman run -d --replace --name todo-web \
  -p 80:8080 \
  -e DB_HOST={{ db_host }} \
  -e DB_PORT={{ db_port }} \
  {{ todo_web_image }}
